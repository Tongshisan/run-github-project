# GitHub AI Agent 详细流程说明

## 🎯 完整工作流程图

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 用户输入                                                  │
│ "找 10 个适合做企业官网的 CSS 动画库"                         │
└────────────────────────┬────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. LLM 分析需求（核心 AI 部分）🤖                            │
│                                                              │
│ 输入给大模型的 Prompt:                                        │
│ ┌────────────────────────────────────────────────────┐    │
│ │ System: 你是 GitHub 搜索助手，分析需求并提取:          │    │
│ │ - 英文关键词                                        │    │
│ │ - 项目数量                                          │    │
│ │ - 编程语言                                          │    │
│ │ - 项目类型                                          │    │
│ │                                                     │    │
│ │ User: "找 10 个适合做企业官网的 CSS 动画库"          │    │
│ └────────────────────────────────────────────────────┘    │
│                         ↓                                   │
│ LLM 思考并返回 JSON:                                         │
│ {                                                           │
│   "keywords": ["CSS", "animation", "library",              │
│                "professional", "business"],                 │
│   "count": 10,                                             │
│   "language": null,                                        │
│   "category": "library",                                   │
│   "description": "适合企业官网的CSS动画库"                   │
│ }                                                           │
└────────────────────────┬────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. 构建 GitHub 搜索查询                                       │
│                                                              │
│ 输入: LLM 返回的 keywords + language + filters              │
│ 输出: "CSS animation library professional business stars:>100"│
└────────────────────────┬────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. 调用 GitHub API                                           │
│                                                              │
│ GET https://api.github.com/search/repositories              │
│ ?q=CSS+animation+library+professional+business+stars:>100   │
│ &sort=stars                                                 │
│ &order=desc                                                 │
│ &per_page=10                                                │
└────────────────────────┬────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 解析和展示结果                                             │
│                                                              │
│ [1] animate.css           82,541 ⭐                         │
│ [2] barba.js             12,815 ⭐                         │
│ [3] aos                   26,789 ⭐                         │
│ ...                                                          │
└────────────────────────┬────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. 用户交互选择                                               │
│ 👉 你的选择: 1                                               │
└────────────────────────┬────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 7. 自动克隆和运行项目                                         │
│                                                              │
│ - 调用 run_github_project.py                                │
│ - 克隆项目                                                   │
│ - 安装依赖                                                   │
│ - 运行项目                                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🤖 大模型具体做什么？

### LLM 的唯一职责：理解和翻译需求

大模型**只在第 2 步**起作用，它的工作是：

#### 输入（Input）
```python
用户的自然语言: "找 10 个适合做企业官网的 CSS 动画库"
```

#### LLM 做的事（Thinking）

1. **理解语义**
   - "找" → 用户想搜索
   - "10 个" → 数量是 10
   - "适合做企业官网的" → 需要专业、商务风格
   - "CSS 动画库" → 技术栈和项目类型

2. **提取关键词**（最重要！）
   - 中文 "CSS 动画库" → 英文 "CSS animation library"
   - "适合企业官网" → "professional business"
   - 去掉停用词："找"、"个"、"的"

3. **识别意图**
   - 项目类型: library（库）
   - 不是 framework、tool 或 example

4. **格式化输出**
   - 生成结构化的 JSON

#### 输出（Output）
```json
{
  "keywords": ["CSS", "animation", "library", "professional", "business"],
  "count": 10,
  "language": null,
  "category": "library",
  "description": "适合企业官网的CSS动画库"
}
```

---

## 📊 对比：有 LLM vs 无 LLM

### 示例查询："找 10 个适合做企业官网的 CSS 动画库"

#### 无 LLM（简单规则）❌

```python
# 步骤 2: 简单分词
keywords = []
exclude = {'找', '个', '的'}
for word in "找 10 个 适合做企业官网的 CSS 动画库".split():
    if word not in exclude:
        keywords.append(word)

# 结果
keywords = ['10', '适合做企业官网的', 'CSS', '动画库']
#            ❌中文    ❌太长一整块      ✓     ❌中文

# GitHub 搜索查询
"10 适合做企业官网的 CSS 动画库 stars:>100"
#  ❌ 中文在 GitHub 搜不到什么结果
```

#### 有 LLM（AI 理解）✅

```python
# 步骤 2: LLM 分析
response = call_llm(
    system="你是 GitHub 搜索助手，提取英文关键词",
    user="找 10 个适合做企业官网的 CSS 动画库"
)

# LLM 返回
{
  "keywords": ["CSS", "animation", "library", "professional", "business"],
  #             ✓英文   ✓英文      ✓英文      ✓理解"适合企业"
  "count": 10,
  "category": "library"
}

# GitHub 搜索查询
"CSS animation library professional business stars:>100"
#  ✅ 全英文，准确，能找到高质量结果
```

---

## 🔍 详细代码流程

### 第 1 步：用户输入
```python
# agent.py 第 389-400 行
query = input("👉 你的需求: ").strip()
# 例如: "找 10 个适合做企业官网的 CSS 动画库"
```

### 第 2 步：LLM 分析（关键！）

```python
# agent.py 第 95-109 行
def analyze_query(self, user_query: str):
    if self.use_llm and self.llm_analyzer:
        # 🤖 使用 LLM 分析
        return self.llm_analyzer.analyze_query(user_query)
    else:
        # 使用简单规则
        return self._simple_analyze(user_query)
```

#### LLM 内部流程：

```python
# llm_analyzer.py 第 40-76 行
def analyze_query(self, user_query: str):
    # 1. 准备 Prompt
    system_prompt = """你是 GitHub 搜索助手，分析需求提取：
    - 英文关键词
    - 数量、语言、类型等
    返回 JSON 格式"""
    
    user_prompt = f"用户需求：{user_query}"
    
    # 2. 调用 LLM API
    if self.provider == "openai":
        result = self._call_openai(system_prompt, user_prompt)
    else:
        result = self._call_anthropic(system_prompt, user_prompt)
    
    # 3. 解析 JSON 结果
    analysis = json.loads(result)
    
    return analysis
```

#### 实际的 API 调用：

```python
# llm_analyzer.py 第 87-110 行
def _call_openai(self, system_prompt, user_prompt):
    data = {
        'model': 'gpt-4o-mini',  # 便宜快速的模型
        'messages': [
            {'role': 'system', 'content': system_prompt},
            {'role': 'user', 'content': user_prompt}
        ],
        'response_format': {'type': 'json_object'},  # 强制返回 JSON
        'temperature': 0.3  # 低温度 = 更确定的结果
    }
    
    response = requests.post(
        'https://api.openai.com/v1/chat/completions',
        headers={'Authorization': f'Bearer {api_key}'},
        json=data
    )
    
    return response.json()['choices'][0]['message']['content']
```

### 第 3 步：构建搜索查询

```python
# agent.py 第 158-173 行
def build_search_query(self, analysis):
    query_parts = []
    
    # LLM 提取的关键词
    if analysis['keywords']:
        query_parts.append(' '.join(analysis['keywords']))
        # ["CSS", "animation", "library"] → "CSS animation library"
    
    # 语言过滤
    if analysis['language']:
        query_parts.append(f"language:{analysis['language']}")
    
    # 最少 star 数
    query_parts.append('stars:>100')
    
    return ' '.join(query_parts)
    # → "CSS animation library professional business stars:>100"
```

### 第 4 步：调用 GitHub API

```python
# agent.py 第 175-224 行
def search_repositories(self, query, count=10):
    url = "https://api.github.com/search/repositories"
    params = {
        'q': query,  # LLM 生成的查询
        'sort': 'stars',
        'order': 'desc',
        'per_page': count
    }
    
    response = requests.get(url, params=params)
    data = response.json()
    
    # 解析结果
    repos = []
    for item in data['items']:
        repos.append(GitHubRepo(
            name=item['name'],
            stars=item['stargazers_count'],
            description=item['description'],
            ...
        ))
    
    return repos
```

### 第 5-7 步：展示、选择、运行

```python
# 展示结果
self.display_results(repos)

# 用户选择
selected_repo = self.interactive_select(repos)

# 运行项目
runner = GitHubProjectRunner(selected_repo.html_url)
runner.run()
```

---

## 💡 总结

### 大模型的作用

**LLM 只做一件事：需求理解和翻译**

1. ✅ 理解中文自然语言
2. ✅ 提取准确的英文关键词
3. ✅ 识别用户意图（类型、数量、语言等）
4. ✅ 输出结构化数据

**LLM 不做的事：**

- ❌ 不直接搜索 GitHub（GitHub API 做）
- ❌ 不克隆项目（run_github_project.py 做）
- ❌ 不安装依赖（shell 命令做）
- ❌ 不运行项目（npm/pnpm 做）

### 为什么要用 LLM？

因为这个：
```
中文: "找 10 个适合做企业官网的 CSS 动画库"
  ↓ LLM 理解语义
英文: "CSS animation library professional business"
  ↓ GitHub 搜索
结果: 准确的专业动画库 ✅
```

简单规则做不到这个！

---

## 🎓 类比

把整个 Agent 比作**旅游向导**：

1. **你**：用中文说"我想去附近好吃的餐厅"
2. **LLM（翻译+理解）**：理解你要找"restaurant nearby good food"
3. **GitHub API（地图搜索）**：在地图上搜索餐厅
4. **展示结果**：给你看 10 家餐厅列表
5. **你选择**：选第 3 家
6. **自动导航**：带你去餐厅（克隆并运行项目）

**LLM 就是那个会翻译、懂你意思的智能助手！**

需要我演示一个实际的 LLM 调用过程吗？

